# Exclude industrial/SCADA protocol rules (Modbus, DNP3) — these
# protocols aren't enabled in this suricata.yaml's app-layer config
# (no ICS/SCADA being simulated in this lab), and Suricata fails hard
# on load if a rule references a disabled protocol.
re:^alert modbus
re:^alert dnp3
